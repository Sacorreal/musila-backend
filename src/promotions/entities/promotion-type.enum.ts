/**
 * Tipo de recurso que se destaca (pauta):
 * - `TRACK`: una canción publicada en la plataforma.
 * - `COMPOSER`: el perfil de un compositor del roster del publisher.
 */
export enum PromotionType {
  TRACK = 'TRACK',
  COMPOSER = 'COMPOSER',
}

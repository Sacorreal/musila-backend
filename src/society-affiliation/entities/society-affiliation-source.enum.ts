/** Origen del dato de afiliación (§4: `source`). Extensible a futuras integraciones (ej. importación CWR). */
export enum SocietyAffiliationSource {
  SELF_DECLARED = 'SELF_DECLARED',
  STAFF_IMPORTED = 'STAFF_IMPORTED',
}

export enum LicenseContractStatus {
  DRAFT = 'draft',
  AWAITING_SIGNATURES = 'awaiting_signatures',
  SIGNED = 'signed',
  FULFILLED = 'fulfilled',
  EXPIRED = 'expired',
  CANCELLED = 'cancelled',
}
